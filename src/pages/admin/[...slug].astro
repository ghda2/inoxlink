---
import AdminLayout from '../../layouts/AdminLayout.astro';
import App from '../../admin/App.vue';

export const prerender = false;
---

<AdminLayout title="CMS Admin">
  <div id="admin-root" class="h-screen w-screen overflow-hidden">
    <App client:only="vue" />
  </div>
</AdminLayout>

<style is:global>
  /* Reset para o painel admin não herdar estilos indesejados do site */
  #admin-root {
    min-height: 100vh;
  }
</style>
